// Placeholder for Stage 4 / Stage 11: enemy spawning and difficulty scaling.
// Will spawn enemies just outside the visible screen on a ring around the
// player, with spawn rate and enemy mix driven by elapsed time.

export class Spawner {
    constructor() {
        throw new Error('Spawner is a placeholder — not implemented until Stage 4.');
    }
}
